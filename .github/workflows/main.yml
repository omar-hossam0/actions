name: First Workflow 

on:
  push:
     branches:
       - main

jobs:
  exp-job:
    runs-on: ubuntu-latest

    steps:
      - name: print welcome message
        run: echo "welcome to my first action workflow"
      - name: checkout repo
        uses: actions/checkout@v6.0.3
      - name: list repo files 
        run: ls -la
